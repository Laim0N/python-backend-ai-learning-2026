def is_adult(age):
    return age >= 18

# def - это ключевое слово для определения функции.
# is_adult - это имя функции (чтоб вызвать функцию, нужно написать is_adult(параметры))
# age - это параметр функции (который мы передаем при вызове функции)
# После : идет тело функции (код, который будет выполняться при вызове функции)
# 